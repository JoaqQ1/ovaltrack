info:
  name: 03 - [404 Not Found] Usuario inexistente
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/user/00000000-0000-0000-0000-000000000000"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debe responder 404 Not Found", function() {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
