info:
  name: 05 - [200 OK] Eliminar persona
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/person/{{newPersonId}}"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debe responder 200 OK y eliminar la persona", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
