info:
  name: 04 - [200 OK] Desvincular entrenador de division
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/coaches/{{newDivisionCoachId}}"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debe responder 200 OK al desvincular entrenador", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
