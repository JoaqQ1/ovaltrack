info:
  name: 06 - [200 OK] Eliminar Club
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/club/{{newClubId}}"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Debe responder 200 OK al eliminar el club", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
